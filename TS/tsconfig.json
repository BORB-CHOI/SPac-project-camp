{
  "compilerOptions": {
    "target": "es6", // 컴파일된 JavaScript의 버전
    "module": "commonjs", // 모듈 시스템 (Node.js 환경에서는 commonjs)
    "strict": true, // 모든 엄격한 타입 검사를 활성화
    "esModuleInterop": true, // ES6 모듈 호환성을 위한 설정
    "skipLibCheck": true, // 라이브러리 파일의 타입 검사를 건너뜀
    "forceConsistentCasingInFileNames": true // 파일 이름의 일관된 대소문자 사용을 강제
  },
  "include": [
    "**/*.ts" // 포함할 파일 경로 (여기서는 src 디렉토리 아래의 모든 .ts 파일)
  ],
  "exclude": [
    "node_modules", // 제외할 파일 경로 (여기서는 node_modules 디렉토리)
    "**/*.spec.ts" // 테스트 파일을 제외할 경우
  ]
}
