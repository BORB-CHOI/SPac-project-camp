const area = "제주도";
let price = 0;

if (area === "서울") {
  price = 3000;
} else if (area === "인천") {
  price = 3000;
} else if (area === "남양주") {
  price = 3000;
} else if (area === "경기도") {
  price = 3500;
} else {
  price = 10000;
}

console.log(price);
